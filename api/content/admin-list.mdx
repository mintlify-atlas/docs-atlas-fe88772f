---
title: "List All Content (Admin)"
api: "GET /api/content/admin/all"
description: "Retrieve all content including drafts (admin only)"
---

## Overview

Admin endpoint to retrieve all content in the system, including draft and published content. This endpoint supports pagination and filtering by status, category, and type.

## Authentication

This endpoint requires admin authentication. Include a valid admin access token in the Authorization header.

```http
Authorization: Bearer <admin_access_token>
```

## Query Parameters

<ParamField query="page" type="number" default="1">
  Page number for pagination
</ParamField>

<ParamField query="limit" type="number" default="10">
  Number of items per page
</ParamField>

<ParamField query="status" type="string">
  Filter by content status: `"draft"` or `"published"`
</ParamField>

<ParamField query="category" type="string">
  Filter by category ID
</ParamField>

<ParamField query="type" type="string">
  Filter by content type: `"pdf"`, `"video"`, or `"audio"`
</ParamField>

## Request

```bash cURL
curl -X GET "https://api.vaniykempire.com/api/content/admin/all?page=1&limit=10" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

```bash Filter by Status
curl -X GET "https://api.vaniykempire.com/api/content/admin/all?status=draft&page=1&limit=20" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

```javascript JavaScript
const response = await fetch(
  'https://api.vaniykempire.com/api/content/admin/all?page=1&limit=10',
  {
    headers: {
      'Authorization': `Bearer ${adminAccessToken}`
    }
  }
);

const data = await response.json();
console.log(`Total content: ${data.totalContent}`);
console.log(`Content items:`, data.content);
```

```python Python
import requests

response = requests.get(
    'https://api.vaniykempire.com/api/content/admin/all',
    headers={'Authorization': f'Bearer {admin_access_token}'},
    params={
        'page': 1,
        'limit': 10,
        'status': 'draft'
    }
)

data = response.json()
print(f"Total content: {data['totalContent']}")
```

## Response

<ResponseField name="content" type="array">
  Array of content objects, sorted by creation date (newest first)

  <Expandable title="content item properties">
    <ResponseField name="_id" type="string">
      Content ID
    </ResponseField>

    <ResponseField name="title" type="string">
      Content title
    </ResponseField>

    <ResponseField name="description" type="string">
      Content description
    </ResponseField>

    <ResponseField name="type" type="string">
      Content type: `"pdf"`, `"video"`, or `"audio"`
    </ResponseField>

    <ResponseField name="category" type="string">
      Category ID
    </ResponseField>

    <ResponseField name="price" type="number">
      Content price in USD
    </ResponseField>

    <ResponseField name="fileUrl" type="string">
      Cloudinary file URL (included for admin)
    </ResponseField>

    <ResponseField name="filePublicId" type="string">
      Cloudinary public ID for file management
    </ResponseField>

    <ResponseField name="thumbnailUrl" type="string">
      Thumbnail image URL (optional)
    </ResponseField>

    <ResponseField name="thumbnailPublicId" type="string">
      Cloudinary public ID for thumbnail
    </ResponseField>

    <ResponseField name="duration" type="number">
      Duration in seconds (for video/audio)
    </ResponseField>

    <ResponseField name="fileSize" type="number">
      File size in bytes
    </ResponseField>

    <ResponseField name="status" type="string">
      Content status: `"draft"` or `"published"`
    </ResponseField>

    <ResponseField name="tags" type="array">
      Array of tag strings
    </ResponseField>

    <ResponseField name="createdBy" type="object">
      Admin user who created the content
      
      <Expandable title="createdBy properties">
        <ResponseField name="_id" type="string">
          Creator's user ID
        </ResponseField>
        
        <ResponseField name="name" type="string">
          Creator's name
        </ResponseField>
        
        <ResponseField name="email" type="string">
          Creator's email
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="createdAt" type="string">
      ISO 8601 timestamp
    </ResponseField>

    <ResponseField name="updatedAt" type="string">
      ISO 8601 timestamp
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="totalPages" type="number">
  Total number of pages available
</ResponseField>

<ResponseField name="currentPage" type="number">
  Current page number
</ResponseField>

<ResponseField name="totalContent" type="number">
  Total number of content items matching the filters
</ResponseField>

### Success Response

```json
{
  "content": [
    {
      "_id": "65f7b3c8e1234567890abcde",
      "title": "Advanced JavaScript Course",
      "description": "Learn modern JavaScript",
      "type": "video",
      "category": "programming",
      "price": 49.99,
      "fileUrl": "https://res.cloudinary.com/demo/video/upload/v1234567890/content/video.mp4",
      "filePublicId": "content/video",
      "thumbnailUrl": "https://res.cloudinary.com/demo/image/upload/v1234567890/thumbnails/thumb.jpg",
      "thumbnailPublicId": "thumbnails/thumb",
      "duration": 3600,
      "fileSize": 524288000,
      "status": "published",
      "tags": ["javascript", "programming", "web-development"],
      "createdBy": {
        "_id": "65f7b3c8e1234567890abc00",
        "name": "Admin User",
        "email": "admin@example.com"
      },
      "createdAt": "2024-03-15T10:30:00.000Z",
      "updatedAt": "2024-03-15T10:30:00.000Z"
    }
  ],
  "totalPages": 5,
  "currentPage": 1,
  "totalContent": 47
}
```

## Error Responses

### 401 Unauthorized

Invalid or missing access token.

```json
{
  "error": "Invalid token"
}
```

### 403 Forbidden

User is not an admin.

```json
{
  "error": "Admin access required"
}
```

### 500 Internal Server Error

```json
{
  "error": "Internal server error message"
}
```

## Notes

<Warning>
  This endpoint returns the `fileUrl` and `filePublicId` fields which are hidden from public endpoints. Only use this endpoint for admin dashboards.
</Warning>

<Info>
  Unlike the public `/api/content` endpoint, this returns both draft and published content. Use the `status` query parameter to filter.
</Info>

<Tip>
  Use pagination to efficiently load large content libraries in admin dashboards.
</Tip>

## Related Endpoints

- [List Content (Public)](/api/content/list-content) - Public content listing
- [Create Content](/api/content/create-content) - Upload new content
- [Update Content](/api/content/update-content) - Modify existing content
- [Delete Content](/api/content/delete-content) - Remove content
